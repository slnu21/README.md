// 처음 만들어 주는 사용자 테마 파일. **의존성 0** — 파서가 이걸 실제로 읽을 수 있는지
// 단위 테스트가 검사하기 위해 따로 둔다(템플릿이 곧 사용자의 첫 경험이다 — 오타 하나면
// 파일을 열자마자 경고 토스트를 보게 된다).

/** 처음 만들어 주는 파일. **이 파일 자체가 설명서다** — JSONC 를 고른 이유가 이것이다.
 *
 *  **예시 테마는 블록 주석 안에 있다.** v0.8.0 까지는 켜진 채로 들어 있어서, 파일을
 *  열어 보려고 버튼을 누른 사람의 테마 목록에 "내 한지" 가 저절로 생겼다(사용자 신고).
 *  샘플은 이 파일이 아니라 `themes\` 폴더 안에 진짜 파일로 있다(themes/seeds.ts). */
export const THEME_FILE_TEMPLATE = `// README.md — 내 테마
//
// **이 파일은 비어 있는 채로 시작합니다.** 아래 /* … */ 를 벗기면 예시 테마가 켜집니다.
// (열어 보기만 한 사람의 테마 목록이 저절로 늘어나지 않게 하려는 것입니다.)
//
// 이 파일을 저장한 뒤 [설정 ▸ 테마 다시 불러오기] 를 누르면 바로 반영됩니다.
//
//   · 색은 6자리 16진수만 씁니다: "#rrggbb" (짧은 "#abc" 도 됩니다)
//     rgb()·색 이름·CSS 함수는 무시되고, 그 항목만 원래 색으로 남습니다.
//   · 주석(// 와 /* */)은 써도 되지만, 마지막 쉼표(,)는 안 됩니다.
//   · 항목을 지우면 그 자리는 자동 계산된 기본값으로 돌아갑니다.
//   · "extends" 로 기본 테마를 바탕에 깔면 바꾸고 싶은 값만 적으면 됩니다.
//   · "id" 를 있는 테마와 같게 하면(light·dark·paper, 그리고 폴더의 한지·전자잉크)
//     그 테마를 덮어씁니다. "extends" 로는 폴더의 팩도 바탕으로 쓸 수 있습니다.
//
// 한지·전자잉크는 [설정 ▸ 테마 폴더 열기] 폴더 안에 **파일로** 있습니다 — 그쪽을 고치거나
// 통째로 베껴 오는 것이 대개 더 빠릅니다. 이 파일은 그 팩들을 **이깁니다**(내가 쓴 것이 이긴다).
//
// 잘못 적어도 앱이 죽지는 않습니다. 무시한 항목은 알림으로 알려 드립니다.
{
  "version": 1,
  "themes": [
    /* ↓ 이 줄과 맨 아래의 주석 닫는 줄, 둘을 지우면 켜집니다.
    {
      "id": "my-theme",
      "name": "내 테마",
      "extends": "paper",   // 바탕 테마 (없으면 light)
      "type": "light",      // "light" | "dark" — 그림자 방향과 색 구성표
      "texture": "none",    // "none" | "hanji" — 리딩 카드의 발(簾) 무늬
      "elevation": "soft",  // "soft" | "flat" — "flat" 은 그림자 없음(전자잉크 느낌)

      // 앱 전체(사이드바·탭·편집기·미리보기·다이어그램)가 이 다섯에서 파생됩니다.
      "tokens": {
        "bg": "#fcf9f2",       // 바탕
        "fg": "#2e2c28",       // 글자
        "accent": "#8a5a2b",   // 강조(링크·선택·다이어그램 노트)
        "surface": "#f6f2e8",  // 바탕보다 살짝 뜨는 면(사이드바·표 머리글)
        "border": "#e9e1d1"    // 테두리
      },

      // 미리보기(리딩 모드) 서식 색. 안 적은 항목은 위 다섯에서 자동으로 나옵니다.
      "prose": {
        "heading": "#241f18",       // 제목 h1~h6
        "headingRule": "#ddd2ba",   // h1·h2 아래 밑줄
        "link": "#8a5a2b",
        "linkUnderline": false,     // true = 항상 밑줄(색이 없는 테마용)

        "quote": "#544c40",         // 인용문 글자
        "quoteBar": "#8a5a2b",      // 인용문 왼쪽 막대
        "quoteBg": "#f5efe2",       // 인용문 바탕

        "code": "#5c3c1c",          // 인라인 코드 글자
        "codeBg": "#f0e6d6",        // 인라인 코드 바탕
        "preBg": "#f4efe4",         // 코드 블록 바탕
        "synKey": "#7a4a1f",        // 코드 강조 — 키워드
        "synStr": "#6b543a",        // 코드 강조 — 문자열

        "marker": "#8a5a2b",        // 목록 불릿·번호·체크박스
        "rule": "#e9e1d1",          // 가로선·표 테두리
        "tableHead": "#f6f2e8",     // 표 머리글 바탕
        "tableZebra": "#f9f5eb",    // 표 짝수 행 바탕
        "markBg": "#eedfae",        // ==형광== 바탕
        "muted": "#615a4e",         // 각주·캡션·h6
        "selection": "#e6d5bd",     // 글자 끌어 선택했을 때

        "note": "#8a5a2b",          // ::: note
        "warn": "#a8641f",          // ::: warning
        "tip": "#3f6f4a",           // ::: tip
        "error": "#a3382c"          // 렌더 오류·삭제된 코드 줄
      }
    }
    ↑ 여기까지 */
  ]
}
`;

/** `themes/` 폴더에 처음 들어가는 안내문. **[테마 폴더 열기]가 이 파일을 선택해 연다** —
 *  그래야 탐색기가 폴더 *안*에서 열리고(부모에서 폴더만 선택되는 게 아니라), 무엇보다
 *  "이 폴더에 뭘 넣지?"에 그 자리에서 답이 된다. `.md` 라 이 앱으로 바로 열어 읽을 수도 있다.
 *  폴더가 비어 있을 때만 만든다 — 지운 사람에게 다시 들이밀지 않는다. */
export const THEME_FOLDER_GUIDE = `# 테마 폴더 / Theme folder

여기에 넣는 것 / What goes here:

| 파일 | 뜻 |
|---|---|
| \`*.jsonc\` | 받은 테마 팩. 넣기만 하면 읽힙니다 — 이게 "가져오기"입니다. |
| \`<테마id>.css\` | 그 테마만의 모양(CSS). 예: \`epaper.css\` |

바꾼 뒤 **설정 ▸ 테마 다시 불러오기**를 누르세요.

## 여기 있는 것들 / What is already here

\`한지\` · \`전자잉크\` · \`컬러 전자잉크\` 는 앱이 넣어 둔 **예시이자 진짜 테마**입니다.
고쳐도 되고, **지우면 목록에서 사라집니다**(다시 만들어 주지 않습니다).
\`컬러 전자잉크\`(\`30-epaper-color.jsonc\` + \`epaper-color.css\`)가 "내 테마 만들기"의 본보기입니다 —
바탕은 그대로 두고 강조 색만 바꾸는, 가장 자주 쓰게 될 방법을 보여 줍니다.

파일 이름 앞의 번호(\`10-\` \`20-\` \`30-\`)는 **읽는 순서**입니다. 같은 id 가 겹치면 뒤가 이깁니다.

## 규칙 넷 / Four rules

1. **색은 \`.jsonc\`, 모양은 CSS.** 색을 CSS에 적으면 다른 테마에서 어긋납니다.
2. **색은 토큰으로.** \`#9c3a2e\` 대신 \`var(--prose-marker)\` 를 쓰면 어느 테마에서나 맞습니다.
3. **토큰이 없는 값은 CSS 맨 위에 모읍니다.** 제목 6단계 색처럼 토큰에 자리가 없는 것은
   \`.md { --h1: …; --h2: …; }\` 로 한 번에 선언하고 아래에서 \`var(--h1)\` 로 씁니다
   (\`epaper-color.css\` 가 그 예입니다). 리터럴을 규칙마다 흩뿌리지 않습니다.
4. **내가 손으로 쓴 것이 이깁니다.** 같은 id면 \`themes.jsonc\` 가 이 폴더의 팩을 이깁니다.

## 쓸 수 있는 선택자 / Stable hooks

\`.md\`(문서 루트) · \`h1\`~\`h6\`(\`id\`=제목 슬러그, \`data-line\`=원본 줄) ·
\`blockquote ul ol li table pre code hr img dl\` · \`.callout\`(\`.note\` \`.warning\` \`.tip\`) ·
\`.footnotes\` \`.footnote-ref\` · \`.task-list-item\` · \`.mermaid-rendered\` · \`.hljs-*\` · \`[data-line]\`

그 밖의 내부 클래스는 예고 없이 바뀔 수 있습니다.

## 예시 — 제목 앞 세로 막대

\`\`\`css
/* <테마id>.css */
h2 { position: relative; padding-left: 16px; }
h2::before {
  content: "";                       /* 글자가 아니라 빈 상자 */
  position: absolute; left: 0; top: .18em; bottom: .18em;
  width: 4px; border-radius: 2px;
  background: var(--prose-marker);   /* 토큰 — 테마를 따라간다 */
}
\`\`\`

원격 주소(웹폰트·이미지 URL)는 오프라인 앱이라 막혀 있습니다. \`data:\` URI 로 넣으세요.
`;
