# OS
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Editors
.idea/
*.swp
*~

# Node (안전망 — src/.gitignore 가 하위에서 처리)
node_modules/
dist/

# Rust / Tauri 빌드 산출물
target/
src/src-tauri/gen/

# 릴리스 바이너리 — Store/GitHub Releases로 업로드(리포에 커밋하지 않음). 텍스트(노트·고지)만 추적.
release/**/*.msix
release/**/*.exe
release/**/*.zip
release/**/*.pfx
release/**/*.cer

# Logs / temp
*.log
