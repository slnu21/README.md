# HTML 시안 (Mockups)

UI 시안을 **단일 .html 파일**로 두고 브라우저로 바로 열어 확인한다(빌드 불필요).

- 시안 파일: `docs/mockups/<이름>.html`
- 시안용 자산(css/img): `docs/mockups/assets/`

> 시안은 디자인 탐색용이며 실제 구현(`src/app`)과 별개다. 확정된 결정은 [design/](../design/)에 반영한다.
